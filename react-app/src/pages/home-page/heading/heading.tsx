import { Typography } from '@mui/material';
import { FC } from 'react';

const Heading: FC = () => {
  return (
    <>
      <Typography variant="h3" sx={{ textAlign: 'center' }}>
        Managing Bugs Becomes Easy
      </Typography>
    </>
  );
};

export default Heading;

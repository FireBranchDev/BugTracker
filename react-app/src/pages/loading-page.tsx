import { FC } from 'react';
import DefaultPageLayout from '../components/default-page-layout';

const LoadingPage: FC = () => {
  return <DefaultPageLayout title="Loading..." />;
};

export default LoadingPage;
